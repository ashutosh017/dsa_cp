#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define forn(i, a, b) for(int i=a;i<=b;i++)
#define ford(i,a,b) for(int i=a;i>=b;i--)
#define rep(i, n) forn(i, 0, n-1)
#define fi first
#define se second
#define vi vector<int>
#define vll vector<ll>
#define vb vector<bool>
#define vvi vector<vector<int>>
#define pb push_back
#define pii pair<int, int>
#define all(x) x.begin(),x.end()
#define nline '\n'
#define mod 1000000007


int fc=0;
vi x(){
    fc++;
    return {1,2,3};
}
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    for(auto i:x())for(auto j:x())for(auto k:x());
    cout<<fc;


    // string s = "123abc123";
    // sort(all(s));
    
    // cout<<s;


     // cout<<floor(3.99)<<ceil(float(3)/2);
    // int a =-4;
    // (a+=a)%=2;
    // cout<<INT64_MAX<<nline;
    // cout<<INT_MAX-(int)1e9-7<<nline;
    // cout<<INT_MAX<<' '<<INT_MAX+1<<' '<<INT_MIN-1;
    // int a = 2*(int)1e9; cout<<a;
    // set<int> s = {1,2,3,4};
    // set<int>::iterator i = s.begin(),j;
    // *i=(*i)-1;
    // cout<<*i;
    // int x =1;
    // while(x++ && x==2)cout<<"yes"; 
    // cout<<x<<nline;   
    // while(x==3 && x++)cout<<"wtf";

    // int x = 10;
    // while(x/=2)cout<<"yes";


    // cout<<(10>>22);
    // cout<<(-1%((int)1e9+7));
    // cout<<(-1)%(100);

    // int a = 4;
    // cout<<16%(a-1);

    // int a = 2;
    // cout<<(1<<a);
    // cout<<a;

    // for(int i{0};i<4;i++)cout<<i<<' ';
    // int a{4};
    // cout<<a;

    // int a =-1,b=-2;
    // cout<<abs(a+b)<<nline;



}



