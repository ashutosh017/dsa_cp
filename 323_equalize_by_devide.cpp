#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define forn(i, a, b) for(int i=a;i<=b;i++)
#define ford(i,a,b) for(int i=a;i>=b;i--)
#define rep(i, n) forn(i, 0, n-1)
#define fi first
#define se second
#define vi vector<int>
#define vll vector<ll>
#define vb vector<bool>
#define vvi vector<vector<int>>
#define pb push_back
#define pii pair<int, int>
#define all(x) x.begin(),x.end()
#define nline '\n'
#define mod 1000000007

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;cin>>t;while(t--){
    	int n;cin>>n;
    	// map<int, int> hash;
    	// vvi ans(3010,vi(2));
    	// vi v(n); rep(i,n)cin>>v[i],hash[v[i]]= i;
    	// int cnt = -1;
    	// while(1){
        //     cnt++;
    	// 	sort(all(v));
        //     if(v[0]==v[n-1])break;
    	// 	if(v[0]==1){
    	// 		cnt=-1;
    	// 		break;
    	// 	}
    	// 	ans[cnt][0] = hash[v[n-1]];
    	// 	ans[cnt][1] = hash[v[0]];
        //     int x = hash[v[n-1]];
    	// 	v[n-1] = (v[n-1]+v[0]-1)/v[0];    		
        //     hash[v[n-1]] = x;
    	// }
        // if(cnt==-1)cout<<-1<<nline;
        // else{
        //     cout<<cnt<<nline;
        //     rep(i,cnt)cout<<ans[i][0]+1<<' '<<ans[i][1]+1<<"\n";
        // }
        
        
    	    	
    }    
}